[project]
name = "codegen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datamodel-code-generator>=0.33.0",
]

[dependency-groups]
dev = [
    "nox>=2025.5.1",
    "pydantic>=2.11.7",
    "pytest>=8.4.2",
]
